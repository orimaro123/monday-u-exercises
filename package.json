{
    "scripts": {
      "heroku-prebuild": "npm i --prefix src/server && npm i --prefix src/client && cp -r src/client/build/ src/server/public",
      "start": "npm start --prefix src/server"
    }
  }