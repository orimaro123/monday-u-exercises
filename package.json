{
    "scripts": {
      "heroku-prebuild": "npm i --prefix src && npm i --prefix src/client && npm run build --prefix src/client && cp -r src/client/build/ src/server/public",
      "start": "npm start --prefix src"
    }
  }